{
  "name": "ai-extension",
  "version": "1.0.0",
  "main": "background.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "build": "npm run build:ts && npm run build:js",
    "build:ts": "tsc",
    "build:js": "esbuild scr/lib/firebase-init.ts --bundle --format=esm --outfile=scr/lib/firebase-init.js --target=es2020",
    "watch": "npm run build:js -- --watch",
    "type-check": "tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build:ts && npm run build:js",
    "build:ts": "tsc",
    "build:js": "esbuild scr/lib/firebase-init.ts --bundle --format=esm --outfile=scr/lib/firebase-init.js --target=es2020",
    "watch": "npm run build:js -- --watch",
    "type-check": "tsc --noEmit",
    "lint": "eslint scr/**/*.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "firebase": "^12.1.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/node": "^24.3.0",
    "autoprefixer": "^10.4.21",
    "esbuild": "^0.25.9",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.12",
    "typescript": "^5.9.2"
  }
}
