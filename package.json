{
  "name": "ai-extension",
  "version": "1.0.0",
  "main": "background.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild lib/firebase-init.ts --bundle --format=esm --outfile=lib/firebase-init.js",
    "watch": "npm run build -- --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "firebase": "^12.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.9"
  }
}
